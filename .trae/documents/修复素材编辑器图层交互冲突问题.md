1. **添加checkClickOnLayer函数**：

   * 检测点击位置是否在图层范围内，即使pointer-events为none

   * 支持判断点击位置在文字层还是底图层

   * 优先检查文字层，然后检查底图层

2. **重构onPreviewAreaMouseDown函数**：

   * 实现统一的鼠标按下事件处理

   * 检测点击位置是否在图层范围内

   * 根据激活状态和点击位置处理拖动逻辑

   * 处理重叠区域的点击和拖动

3. **修复滚轮事件处理**：

   * 确保当激活底图层时，滚轮事件能正确触发底图层缩放

   * 确保当激活文字层时，滚轮事件能正确触发文字层缩放

   * 修复onPreviewAreaWheel、onImageWheel和onTextWheel函数

4. **优化图层交互逻辑**：

   * 确保激活状态正确影响鼠标事件处理

   * 修复拖动事件的传播问题

   * 确保元素激活时能正确响应鼠标操作

5. **保持变量命名和文件结构不变**：

   * 按照当前项目的变量命名规范实现

   * 保持material-interactions.js的文件结构

   * 不修改其他文件的代码

